<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord - Administrateur</title>
</head>
<body>
    <header>
        <!-- Ajouter logo -->
        <h1>Tableau de bord</h1>
    </header>
    <main>
        <p>A partir de cette page, vous pourrez gérer les utilisateurs, les pontons et les réservations.</p>
        <div>
            <h2>Gestion des usagers</h2>
            <p>Cliquez ici pour afficher la liste de tous les utilisateurs, administrateurs ou clients :</p>
            <button><a href="/users">Liste des utilisateurs</a></button>
        </div>
        <div>
            <h2>Gestion des pontons</h2>
            <p>Cliquez ici pour afficher la Liste de tous les pontons du port :</p>
            <button><a href="/catways">Liste des pontons</a></button>
        </div>
        <div>
            <h2>Gestion des réservations</h2>
            <p>???</p>
        </div>

        <button id="logout-btn">Se déconnecter</button>
    </main>
    <footer>
        <!-- Lien mentions légales et contact -->
    </footer>
    <script>
        async function fetchDashboard() {
            const token = localStorage.getItem("token");
            console.log("Token récupéré de localStorage :", token);

            if (!token) {
                alert("Token non trouvé ! Veuillez vous connecter.");
                window.location.href = "/";
                return;
            }

            console.log("Token envoyé:", token);

            try {
                const response = await fetch("/users/admin-data", {
                    method: "GET",
                    headers: {
                        "Authorization": `Bearer ${token}`
                    }
                });

                console.log("En-têtes de la requête : ", {
            "Authorization": `Bearer ${token}`});


            } catch (error) {
                console.error("Erreur lors de la requête API", error);
            }
        }

        function logout() {
            localStorage.removeItem("token");
            window.location.href = "/";
        }
        
        document.getElementById("logout-btn").addEventListener("click", logout);

        fetchDashboard();
    </script>
</body>
</html>
